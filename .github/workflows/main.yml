name: Email Action Test

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f3000994@ds.study.iitm.ac.in
        run: echo "Hello, world! This action was triggered by $GITHUB_ACTOR"
